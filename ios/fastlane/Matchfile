git_url("https://github.com/chintheman/toto-ios-certs.git")
storage_mode("git")

# App Store distribution certs/profiles only — this app has no ad hoc or
# enterprise distribution use case.
type("appstore")

app_identifier(["com.chintheman.toto"])

# MATCH_PASSWORD (the passphrase match encrypts this repo's contents
# with) is never written here or anywhere in git — it's injected as an
# env var from 1Password at run time. See fastlane/README.md.
